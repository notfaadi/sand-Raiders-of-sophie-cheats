# Brand Studio / __brand are blocked in workers/site.js (and functions/_middleware.js).
# Workers static assets reject status 404 in this file -- do not add 404 rules here.

# Legacy sitemap URLs -> primary index (also enforced in workers/site.js)
/sitemap-index.xml /sitemap.xml 301
/sitemap-0.xml /sitemap.xml 301
/sitemap.xml/ /sitemap.xml 301
/sitemap-en.xml/ /sitemap-en.xml 301
/sitemap-i18n.xml/ /sitemap-i18n.xml 301
/sitemap-images.xml/ /sitemap-images.xml 301

# HTTPS + www -> apex are enforced in workers/site.js / functions/_middleware.js (301).
# Cloudflare DNS: CNAME www → sandraiderscheats.net (proxied) OR Redirect Rule
#   Source: www.sandraiderscheats.net/* → https://sandraiderscheats.net/${1} (301).
# Dashboard: attach custom domain sandraiderscheats.net, enable "Always Use HTTPS".
